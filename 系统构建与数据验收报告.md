# 管道应力多传感数据平台——系统构建与数据验收报告

## 1. 本次交付结论

已构建可运行的数据治理与处理底座，并用 P110“实验2—各探头四点弯牵拉对比”全量数据完成验证。系统不修改桌面源文件；原始内容按 SHA-256 去重保存，磁信号、应变时序、应变取数、标签、质量控制和特征分别版本化。

当前冻结数据集：

```text
dataset_id:              P110_EXP2_20260819_21
dataset_version:         1.0.0+97b0dca62768
dataset_fingerprint:     97b0dca627686ad7002a6cbb83348017963a6d7dbca24d0030978cc50c2010c5
pipeline_version:        0.2.0
pipeline_code_sha256:    dfbd39af277a3c6bff369b5fe3b7491ca4f3a7e6509af469aeef41124b04fbc7
signal_standardization:  8fd95fd8803f
```

第二次使用相同输入增量运行耗时约 2.85 s，数据集指纹保持完全一致。

## 2. 已处理的真实数据

| 对象 | 数量 | 处理结果 |
|---|---:|---|
| 源文件 | 211 | 全部登记路径、角色、大小、时间和 SHA-256 |
| 唯一原始内容 | 201 | 内容寻址保存，10份重复内容自动去重 |
| 磁牵拉 CSV | 53 | 全部成功解析和标准化 |
| 连续应变 XLSX | 9 | 53867条时序记录、174通道；绕过错误的A1 used-range |
| 应变取数 XLSX | 6 | 31条阶段记录、174通道 |
| 应变取数 TXT | 3 | 14条阶段记录、174通道 |
| 标准应变值 | 9380688 | 183个未平衡/非数值保留为null并带QC，未填0 |
| 磁通道特征行 | 639 | 逐扫描、逐传感器、逐轴，可追溯到零载基线 |
| AI样本行 | 53 | 一次牵拉一行，标签和分组键显式保存 |
| 地标/几何事件 | 318 | 进管、出管自动事件及压头/支墩几何先验 |

应变通道方向按已确认规则登记：`-1=环向`、`-3=轴向`；`30-2` 等非标准方向保留为 `unknown`，没有强行改成轴向或环向。

## 3. 质量门禁结果

独立验证共40项，结果为 `PASS`，包括：表可读性、主外键、分区存在性、scan_id唯一性、基线外键、盲态标签隔离、MPa闭锁和201个不可变原始副本的全量SHA-256复核。

所有53次磁扫描的文件/结构处理成功，但总体状态为 `WARN`，原因是原文件未记录采样率、牵拉速度和提离。系统因此只提供采样域/归一化管长特征，不把频率解释成Hz，也不把归一化位置解释成精确米制坐标。

主候选轴按用户确认的列序 `Z,Y,X` 进行削顶检查：

| 探头方案 | 主轴通过 | 主轴拒判 | 说明 |
|---|---:|---:|---|
| 完整MEM | 2 | 12 | 多数文件X轴达到量程轨，文件仍保留供Z/Y及审计使用 |
| 单边开槽MEM | 13 | 0 | X轴通过当前门限；仍缺跨轮加载重复性 |
| 零磁 | 5 | 0 | X轴通过，但其他轴仍可能存在削顶警告 |
| 剩磁—零磁磁化节 | 0 | 11 | 当前X轴主特征拒判 |
| 剩磁—开槽MEM磁化节 | 0 | 10 | 当前X轴主特征拒判 |

该门禁不等于“完整MEM物理效果差”。它表示：在当前轴定义和±36000量程规则下，这些具体文件不能直接进入以X轴为主的定量模型。已有其他轴或组合特征仍可作为候选，但必须显式记录所用通道并重新做跨轮验证。

## 4. 数据链已实现的能力

1. 不可变原始层：SHA-256内容寻址、重复内容去重、源路径和原生应变仪工程文件保留。
2. 配置驱动解析：探头别名、3/45列ZYX布局、有效传感器白名单、轮次和特殊阶段从JSON读取。
3. 标签隔离：应力真值不再硬编码在算法里，盲测标签可以为空并被程序闭锁。
4. 应变标准化：流式读取错误used-range的XLSX；原始时序和阶段取数统一成长表。
5. 信号标准化：保留原始值、哨兵、削顶、管内标志和归一化位置，不用滤波结果覆盖原始值。
6. QC分层：解析成功、通道质量、主应力候选轴和定量输出许可分别判断。
7. 特征血缘：基础统计、尖峰、信息熵、Q60–Q80零载差和CDF优势度记录特征版本、基线scan_id和代码哈希。
8. AI防泄漏：`split_group=pipe_id+experiment_id+run_id`；同轮阶段不会随机拆入训练和测试。
9. 双格式目录：CSV供人工复核，Parquet供MATLAB/Python/AI，SQLite供查询。
10. 冻结发布：配置、标签、原始文件或代码变化都会生成新的数据集指纹和release。

## 5. 新数据的标准流程

```text
新实验目录
  → 复制实验配置模板
  → 登记管道/探头/通道/工况/标签
  → ingest + SHA-256归档
  → schema与QC门禁
  → 标准信号/应变/事件
  → 零载相对特征和AI矩阵
  → 独立validate
  → 冻结dataset_version
```

新实验必须新增 `experiment_id`，同一根管道保持同一 `pipe_id`。位移、时钟方向、技术复测、恢复阶段和加载/反转分支进入manifest，不能依赖文件夹排序。

## 6. 尚未完成的生产化能力

当前版本是可运行、可追溯的工业化底座，不应被描述为已经部署的企业生产平台。正式现场使用前还需要：

- 为406磁/剩磁和ETP建立独立adapter，并把奇偶列、复数涡流相位和通道单位写入注册表；
- 同步编码器或稳定速度，建立真正米制空间坐标和焊缝/压头/支墩的信号检测事件；
- 补齐采样率、磁化电流、提离、探头序列号、温度、姿态和校准证书；
- 把双边完整、单边开槽和未来双边对称开槽作为正式 `probe_config_id`，不能只写显示名称；
- 将 `lake/raw` 部署到带版本锁/备份的NAS或对象存储，并增加权限、审计、备份恢复和容量监控；
- 建立容器/依赖锁、CI回归测试、模型注册、预测审计和人工复核工作流；
- 在多根独立管道盲测完成前，保持 `direct_mpa_output_allowed=false`。

## 7. MATLAB验证状态

已提供 `matlab/loadStressDataset.m`，接口按 `dataset_index.json` 解析相对路径，不依赖桌面中文目录。Python全链及Parquet/SQLite输出已实际通过测试；本轮自动启动MATLAB时受到执行环境的本机进程限制，尚未完成MATLAB运行时复核。可在本机MATLAB中执行：

```matlab
addpath('matlab');
d = loadStressDataset('lake/dataset_index.json');
assert(height(d.scans)==53);
```
