## P110 数据模态边界（重要）

当前 P110 EXP2 数据集只包含三轴磁传感数据和应变片数据，不包含独立采集的剩磁信号数据，也不包含涡流 ETP 数据。探头或文件夹名称中涉及“剩磁/磁化节”的文字仅为实验硬件或磁化工况命名；对应 CSV 仍是三轴磁传感器输出，不能解释为另一套剩磁测量模态。

## 内容

- 可追溯管道应力实验数据处理平台；
- 匿名、确定性 P110 类磁信号阅读者 Demo 及独立应变标签；
- [P110 真实磁传感+应变一级案例](../case_studies/p110_magnetic_strain/README.md)：冻结派生表、真实结果图、Python/MATLAB 重绘程序、完整脱敏配置和失败闭锁校验；
- Python/MATLAB 数据读取接口；
- 数据质量、血缘、盲态隔离与版本指纹验证；
- 经明确授权公开的 P110 EXP2 全量数据附件：原始快照、标准化磁信号、应变、特征、目录和校验信息。

## 全量附件校验

- 文件：`P110_EXP2_full_release_1.0.0+97b0dca62768.zip`
- 大小：169,639,531 字节
- SHA-256：`38cc098c9a590f05efea429073e7c17c0b2c5f8b0232a7625e50957e05fa6a7c`

解压后以 `dataset_index.json` 为唯一程序入口，不要通过文件名重新猜测工况。新增实验应生成新配置、独立标签和新数据集版本，禁止覆盖本 Release。

全量包的 40 项 `PASS` 只表示文件完整性与结构一致性校验通过，不表示应力模型性能通过。案例目录内的 10 行表是经审查的真实派生证据，但目前尚未包含逐行扫描 ID、源哈希和方法版本，因此不得声称它已实现从全部原始数据独立重建。

## 工程边界

当前磁特征适合应力相关性、相对分级和应力集中筛查。未经新管道标定、独立盲测和不确定度验证时，不得直接解释为绝对应力 MPa。
